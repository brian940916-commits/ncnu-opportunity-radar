# /check-opportunities

主要功能：幫使用者找出現在最該關注的機會。

**⚠️ 重要：這是一個 Agent 任務，不是線性腳本。**
你（執行這個 command 的 AI）要自己規劃流程，而不是照固定 step 跑。

---

## 你的角色

你是一個有自主判斷力的 Agent。你的工作不是「按順序跑五個函式」，而是：

1. **理解使用者**：讀 user_profile，推測他現在的狀態
2. **規劃策略**：決定要爬哪些來源、用什麼關鍵字、要不要做第二輪
3. **執行工具**：呼叫 scrape_school、search_web、filter_ai
4. **檢視結果**：看結果好不好，不好就調整再試
5. **產出輸出**：用 Markdown 把結果呈現給使用者
6. **學習記錄**：把這次行為寫進 history，推到 GitHub

---

## 可用工具

| Tool | 能力 | 何時用 |
|---|---|---|
| `read_profile()` | 讀取 user_profile.json | 一開始一定要做 |
| `scrape_school(urls)` | 爬暨大固定頁面 | 想要學校官方資訊時 |
| `search_web(queries)` | DuckDuckGo 搜尋 | 想找外部機會時 |
| `filter_ai(profile, raw)` | AI 過濾與評分 | 拿到 raw 資料後 |
| `check_cache()` | 看有沒有 24h 內的快取 | 想省時間時（選做） |
| `save_history(profile, results)` | 把這次結果寫回 profile | 最後一定要做 |
| `git_sync()` | commit + push profile 到 GitHub | 最後一定要做 |

---

## 任務啟動流程

當使用者輸入 `/check-opportunities` 時，依以下方式工作：

### 第一階段：理解與規劃（30 秒）

1. 讀取 `user_profile.json`
2. 如果 `user_profile.json` 不存在或缺欄位 → 提示使用者先跑 `/init-profile`
3. 看 `history.clicked` 最近 10 筆，**自己用一段話總結**：
   - 「使用者最近偏好 XX 類型」
   - 「使用者最近活躍度高/中/低」
   - 「使用者似乎處於 XX 階段」
4. 根據這個總結，**自己決定**：
   - 要不要全爬四個學校 URL？還是只爬最相關的？
   - 要用幾組搜尋關鍵字？哪些優先？
   - 推薦數量目標：3 / 5-7 / 8-10 則

**把你的規劃用一行字告訴使用者**，例如：

```
📍 規劃：你最近在找比賽，我會加強搜尋黑客松，並用 5 組關鍵字。
```

### 第二階段：並行收集資料（30 秒）

依照規劃，並行（用 asyncio 或 concurrent.futures）呼叫：
- `scrape_school(urls)` 抓學校資料
- `search_web(queries)` 抓外部資料

**過程中即時回報進度**：
```
⏳ 爬學校網站... ✓ 24 筆
⏳ 外部搜尋... ✓ 18 筆
```

### 第三階段：AI 判斷與評分（20 秒）

呼叫 `filter_ai(profile, raw_data)` 做：
- 情境推理
- 資格匹配
- 時效檢查（四層機制的第四層）
- 相關性評分
- 跨來源去重與加分
- 信心度標記

### 第四階段：自我檢視（10 秒）

**這是 Agent 的關鍵：看結果好不好，不好要再做一次。**

檢查指標：
- 高優先（score > 80 且未過期）數量 < 2？→ 太少，換關鍵字再搜一次
- 全部都是同一類型（例如全是黑客松）？→ 用其他類型關鍵字補搜
- 信心度都很低（confidence < 60 平均）？→ 在輸出中說明「這次找到的不太確定，建議手動調整 profile」

如果觸發任何一個條件，**回到第二階段做一次補強搜尋**。

### 第五階段：輸出

用 Markdown 格式呈現（見下方範本）。

### 第六階段：記錄與同步

1. 寫入 `history.last_check_results`（這次推薦了什麼）
2. 更新 `last_check` 時間戳
3. 呼叫 `git_sync()` 推到 GitHub

---

## 輸出格式範本

```markdown
🎯 機會雷達 · 2026-06-04 14:32

📍 規劃：你最近偏好比賽類，我加強搜了黑客松，總共 42 筆原始資料。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔥 高優先（截止 < 7 天且高度相關）

1. **台積電 CareerHack 黑客松** ⭐87
   [學校 ✓] [搜尋 ✓ 已驗證] 截止 6/10（剩 6 天）
   💡 AI 理由：符合資工大三背景，含實習面試直通機會，興趣命中：黑客松、AI
   🔗 https://...

2. **教育部圓夢獎學金** ⭐82
   [搜尋 ✓] 截止 6/08（剩 4 天）
   💡 AI 理由：符合一般生身份，金額 5 萬
   ⚠️ 信心 65%，原因：截止日是從網頁推測的，請手動確認
   🔗 https://...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📅 本月內（截止 7-30 天）

3. **AWS 學生大使招募** ⭐75
   [搜尋 ✓] 截止 6/30
   💡 雲端技術相關，符合 AI 興趣
   🔗 https://...

4. **國科會暑期專題計畫** ⭐72
   [學校 ✓] 截止 6/25
   💡 學期實習機會，含薪資
   🔗 https://...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 本次統計：原始 42 筆 → 過濾後 18 筆 → 高分推薦 4 筆
⏱️ 耗時 1 分 12 秒

💬 互動指令：
  「展開 #N」    看完整推理過程
  「已申請 #N」  標記為已申請
  「不要 #N」    這類不要再推
  「重跑」       換個方向再搜一次
```

### 輸出格式的設計原則

- **資訊密度高但好讀**：每則機會 3-4 行就要看完關鍵資訊
- **emoji 用來分區**，不要亂撒
- **截止日要算「剩幾天」**，使用者一眼看得到緊迫性
- **AI 理由要短**（1-2 句），長的推理放「展開 #N」才秀
- **信心度低的要標紅旗**（⚠️），不要假裝什麼都很確定

---

## 互動指令處理

使用者看完輸出後可能會輸入這些指令：

### 「展開 #N」
顯示這則機會的 `reasoning_detail` 完整內容，包含 AI 為什麼這樣評分。

### 「已申請 #N」
- 寫入 `history.applied`
- 把該類別權重 +1
- git push 同步
- 回覆「👍 已記錄，會多推類似的」

### 「不要 #N」
- 把該則的關鍵字加入 `avoid`
- 寫入 `history.skipped`
- git push 同步
- 回覆「👌 之後不會推這類」

### 「重跑」
- 不用 cache
- 隨機改變 30% 的搜尋關鍵字（探索新領域）
- 重新跑一次完整流程

### 「點開 #N」
- 寫入 `history.clicked`（記時間戳）
- 開始計算停留時間（選做）
- 顯示完整 URL 給使用者打開

---

## 錯誤處理

### user_profile.json 不存在
```
❌ 找不到 user_profile.json
請先執行 /init-profile 建立你的個人檔案。
```

### 學校網站爬不到
```
⚠️ 學校網站暫時無法存取（status: 500）
我先用外部搜尋的結果給你看，等等可以再試一次。
```

### 沒有任何結果
```
🤔 這次沒找到符合條件的機會。

可能原因：
- 你的 avoid 清單太嚴格
- 興趣關鍵字太冷門
- 現在不是釋出機會的旺季

建議：
1. 試試輸入「重跑」用更廣的搜尋
2. 或執行 /init-profile 調整偏好
```

### Git push 失敗
```
⚠️ GitHub 同步失敗（[錯誤訊息]）
這次的結果已存在本地，下次執行時會再試。
```

---

## ⚠️ 重點提醒（給實作這個功能的 Claude Code）

1. **絕對不要寫成寫死的 step 1→2→3→4→5 流程**。即使最後實作起來看似有 5 個步驟，**這 5 個步驟內部要有判斷邏輯**（例如「如果結果太少就回頭再搜」）。

2. **AI 規劃要可見**。使用者要看到「📍 規劃：...」這一行，這證明這是 Agent 不是腳本。

3. **錯誤要說人話**。不要讓使用者看到 Python traceback，要轉成人類能看懂的訊息。

4. **每次都要 git push**。這是專案核心設計，profile 一定要同步。

5. **時間顯示要友善**。「剩 6 天」比「2026-06-10」更直覺。
